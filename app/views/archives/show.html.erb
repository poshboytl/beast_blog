<% @posts.each do |month, posts| %>
  <div>
    <h2><%= month.strftime("%Y/%m") %></h2>
    <ul>
      <% posts.each do |post| %>
        <li>
          <%= link_to post_title(post), post_path_with_slug(post) %>
        </li>
      <% end %>
    </ul>
  </div>
<% end %>

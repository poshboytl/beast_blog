<ul>
  <% @posts.each do |post| %>
    <h5><%= link_to post_title(post), post_path(id: post.id) %></h5>
    <p><%= post.author.name %>, <%= l post.created_at, format: :date_only %></p>
  <% end %>
</ul>
